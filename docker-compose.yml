version: '3.7'

services:
  db:
    image: postgres
  cnab_parser:
    build: .
    volumes:
      - .:/cnab_parser
      - gems:/gems
    ports:
      - "3000:3000"
    depends_on:
      - db
volumes:
  gems:
